/*
Representacao de um espaço na memoria, usado para transitar dados de forma rapida
Classe para manipula dados binarios de maneira direta, em situações que precisa lidar com dados de forma eficiente

    - Exemplo
*/

const buf = Buffer.from("Hello");
console.log(buf);
